spring:
  datasource:
    url: ${DB_URL:${NEON_DB_URL}}
    username: ${DB_USERNAME:${NEON_DB_USERNAME}}
    password: ${DB_PASSWORD:${NEON_DB_PASSWORD}}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: ${FLYWAY_ENABLED:true}
